// Single re-export point for the generated Prisma 7 client (output configured
// in prisma/schema.prisma → ../src/generated/prisma). Import PrismaClient,
// model types (User, AppSettings) and enums (UserStatus, UserRole) from here so
// the generated output path lives in exactly one place.
export * from '../generated/prisma/client';
